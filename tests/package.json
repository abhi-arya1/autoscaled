{
  "name": "autoscaled-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test test.ts",
    "test:watch": "bun test --watch test.ts",
    "test:worker": "cd worker && bun run dev"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}

